import historyIcon from '../assets/history-icon.svg';

function RecentSection() {
  return (
    <div className="recent h-auto opacity-50">
      {/* <img src={historyIcon} alt="History" className="historyicon" /> */}
      <span >Recent</span>
    </div>
  );
}

export default RecentSection;
// This component serves as a section in the sidebar for displaying recent activity.
// It includes an icon representing history and a label, enhancing the user interface and navigation experience.